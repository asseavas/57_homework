export interface User {
  name: string;
  email: string;
  activity: boolean;
  role: string;
}

export interface UserMutation {
  name: string;
  email: string;
  activity: boolean;
  role: string;
}